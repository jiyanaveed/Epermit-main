The scraper/agent pipeline shows "No portal credentials found for 
this project" because the credential matching logic requires 
credentials to be linked to a specific project. But we removed the 
"Link to Project" field from the credentials form.

Fix the credential matching logic so it works by JURISDICTION 
instead of project linking. Here is how it should work:

1. When the user clicks "Run Manual Check" on the dashboard, the 
   system knows which project is selected (from the sidebar) and 
   its permit number.

2. Each project has a jurisdiction field (or the user selected a 
   jurisdiction when creating the project). Use this jurisdiction 
   to find matching credentials.

3. The credential lookup logic should be:
   a) Get the selected project's jurisdiction name
   b) Find credentials in the portal_credentials table where 
      the jurisdiction field matches (case-insensitive)
   c) Use those credentials (username, password, portal_url) 
      for the scraper login

4. If no jurisdiction match is found, fall back to checking if 
   there are any credentials where the portal_url domain matches 
   the project's known portal URL.

5. If still no match, show the error: "No portal credentials 
   found for jurisdiction [X]. Please add credentials in Settings."

Check these files for the credential matching logic:
- src/components/dashboard/AgentWorkflowStatus.tsx (where Run 
  Manual Check triggers)
- Any API endpoint that fetches credentials for a project
- scraper-service/server.js (if it receives credentials)

The previous logic used project_id to match credentials. Change it 
to use jurisdiction name instead. Make sure the credentials table 
query uses .ilike() or case-insensitive matching on the jurisdiction 
column.

Also check: does the projects table have a jurisdiction column? If 
not, the jurisdiction might be stored elsewhere (like in the sidebar 
selection or in the credential itself). Trace how the current 
Washington DC credential was previously matched to understand the 
existing data flow, then fix it.

Do NOT change the credential form — it is correct now. Only fix 
the matching/lookup logic.