Context: We need to fully align the platform with our formal ESG and Go/No-Go Decision Frameworks. The system must track explicit human timing, measure progress against strict volume gates, evaluate agents against individual accuracy thresholds, and flag high-risk "confident but wrong" errors.Requested Actions:1. Implement Human Time Tracking (Expeditor Workflow):
* Requirement: We must capture "Time spent per comment" to establish our baseline.Action: Add a background timer or a "Start/Stop Review" widget to the human expeditor's grading/reconciliation UI (where they Accept/Modify/Fail the AI). When the human submits their decision, calculate the elapsed time in minutes and save it to a duration_minutes column in the baseline_actions or shadow_predictions table. Update the "Baseline Metrics" ribbon on the Shadow Mode Dashboard to calculate the true "Avg Time per Comment" based on this new data.2. Build the "Go/No-Go Readiness" Tracker (Dashboard UI):
* Requirement: The framework requires $\ge 300$ comments processed and $\ge 30$ projects processed in Shadow Mode to pass the validation gate.Action: Add a new "Validation Gate Progress" section to the ShadowModeDashboard. Include two visual progress bars tracking global database totals:Total Shadow Comments: [Current Count] / 300Total Shadow Projects: [Current Count] / 303. Implement Agent-Specific Accuracy Thresholds:Requirement: Agents have different passing grades.Action: Update the "Agent Performance" component. Instead of a generic color scheme, evaluate and visually flag (Pass/Fail or Green/Red) each agent against its strict framework minimum:
* Comment Parser: $\ge 90\%$ * Guardian Agent: $\ge 90\%$ * Discipline Classifier: $\ge 85\%$ * Code Reference: $\ge 80\%$ * Similar Matcher: $\ge 75\%$ * Response Matrix Gen: $\ge 70\%$ 4. Flag "Confident But Wrong" Errors (Risk Alert):Requirement: We must identify calibration issues where the AI is highly confident but incorrect.Action: In the Prediction Comparison Table, add a visual alert (e.g., a red warning icon or highlighted row) for any row where match_status === 'mismatch' AND confidence_score >= 80%. Add a new interactive filter card at the top called "High-Risk Errors" that isolates only these specific, dangerous edge cases.